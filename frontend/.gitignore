# -----------------------------------------------------------------------------
# 前端子项目（与仓库根目录 .gitignore 对齐；在 frontend/ 内单独打开工程时同样生效）
# -----------------------------------------------------------------------------

# 依赖
node_modules/

# 构建产物
dist/
dist-ssr/

# TypeScript 增量缓存
*.tsbuildinfo

# 本地环境（可保留 .env.development 入仓时勿填密钥；敏感项用下列文件）
.env.local
.env.*.local

# 日志与系统
*.log
.DS_Store
Thumbs.db
